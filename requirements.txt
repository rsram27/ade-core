# ADE Core - Minimal Requirements

# MCP Server (Model Context Protocol)
mcp>=1.2.0

# Configuration
pyyaml>=6.0

# HTTP client (for Databricks API)
requests>=2.31.0

# Web UI (optional — needed for streamlit_app)
streamlit>=1.30.0
