# ADE Demo Environment Configuration
# This is a synthetic demo environment with sample data

environment:
  name: "ADE Demo"
  description: "Synthetic demo environment for testing and learning"
  mcp_enabled: true

platforms:
  databricks:
    enabled: true
    description: "Sample Databricks notebooks and jobs"

  powerbi:
    enabled: true
    description: "Sample Power BI semantic model (Acme Sales)"
  postgresql:
    enabled: false
